---
interface Props {
  source: string;
  autoplay?: boolean;
  controls?: boolean;
  muted?: boolean;
  loop?: boolean;
  mediaType?: string;
}

const { source, autoplay = true, controls = true, muted = false, loop = false, mediaType = "video/mp4" } = Astro.props;
---

<div class="w-full flex justify-center">
  <video
    class="w-full h-auto aspect-video rounded-lg"
    { ...(autoplay ? { autoplay: "true" } : {}) }
    { ...(controls ? { controls: "true" } : {}) }
    { ...(muted ? { muted: "true" } : {}) }
    { ...(loop ? { loop: "true" } : {}) }
  >
    <source src={source} type={mediaType} />
  </video>
</div>
